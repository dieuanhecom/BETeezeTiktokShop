allowed_filter_operations = [
    "$gte",
    "$lte",
    "$gt",
    "$lt",
    "$exact",
    "$in",
    "$contains",
    "$icontains",
    "$startswith",
    "$istartswith",
    "$endswith",
    "$iendswith",
    "$range",
    "$isnull",
    "$regex",
    "$iregex",
    "$eq",
]

allowed_sort_operations = ["asc", "desc"]
